{
  "name": "vici-frontend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "cd packages/shared && npm run test",
    "lint": "cd packages/shared && npm run lint",
    "build": "cd packages/shared && npm run build"
  },
  "dependencies": {
    "turbo": "^2.5.0"
  }
}
