/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './src/App';

// Use a hardcoded app name instead of importing from app.json
const appName = 'vicimobile';

AppRegistry.registerComponent(appName, () => App); 